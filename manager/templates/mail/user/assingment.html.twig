{% extends 'mail.html.twig' %}

{% block title %}Profile{% endblock %}

{% block body %}
    <p>Hello,{{ executor.name }}</p>
    {% set url = url('work.projects.tasks.show',{'id': task.id}) %}
    <p>Follow the link to see the task<a href="{{ url }}">{{ url }}</a></p>
{% endblock %}